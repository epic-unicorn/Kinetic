name: kinetic_qr_scanner
description: FOSS QR scanner (ZXing-C++) for Kinetic Link — no ML Kit / Play Services.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter
  zxing_barcode_scanner: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
